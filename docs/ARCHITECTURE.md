# OpenClaw Docker 架构文档

**最后更新：** 2026-02-06  
**版本：** 1.0.0

本文档详细说明 OpenClaw Docker 部署的完整基础设施架构，包括网络拓扑、组件交互、通信机制和代理配置。

---

## 📋 目录

1. [架构概览](#架构概览)
2. [网络拓扑](#网络拓扑)
3. [核心组件](#核心组件)
4. [通信流程](#通信流程)
5. [数据流分析](#数据流分析)
6. [安全机制](#安全机制)
7. [部署环境](#部署环境)

---

## 架构概览

### 整体架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                          外部访问层                                   │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  Internet Users                    Telegram Users                    │
│       │                                   │                          │
│       │                                   │                          │
│       ▼                                   ▼                          │
│  ┌──────────────┐                  ┌──────────────┐                │
│  │  Cloudflare  │                  │   Telegram   │                │
│  │    Tunnel    │                  │   Platform   │                │
│  │   (公网入口)  │                  │  (Bot API)   │                │
│  └──────┬───────┘                  └──────┬───────┘                │
│         │                                  │                          │
└─────────┼──────────────────────────────────┼──────────────────────────┘
          │                                  │
          │ HTTPS                            │ HTTPS
          │                                  │
┌─────────┼──────────────────────────────────┼──────────────────────────┐
│         │      Docker Network (openclaw_net)       │                  │
│         │                                  │                          │
├─────────▼──────────────────────────────────▼──────────────────────────┤
│                          反向代理层                                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌──────────────────────────────────────────────────┐               │
│  │              Traefik v3.0                        │               │
│  │         (Reverse Proxy & Router)                 │               │
│  ├──────────────────────────────────────────────────┤               │
│  │  Port 80:   HTTP  Entrypoint                     │               │
│  │  Port 443:  HTTPS Entrypoint (TLS Termination)   │               │
│  │  Port 8080: Dashboard (管理界面)                  │               │
│  └──────────┬───────────────────────────────────────┘               │
│             │                                                         │
│             │ HTTP (内部)                                             │
│             │                                                         │
├─────────────▼─────────────────────────────────────────────────────────┤
│                          应用服务层                                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌─────────────────────────────────────────────────┐                │
│  │          OpenClaw Gateway                       │                │
│  │      (Core AI Agent Service)                    │                │
│  ├─────────────────────────────────────────────────┤                │
│  │  • Gateway Core:    Port 18789 (内部)           │                │
│  │  • Socat Proxy:     Port 18790 (暴露)           │                │
│  │  • Web UI:          /__openclaw__/canvas/       │                │
│  │  • WebSocket:       ws://127.0.0.1:18789        │                │
│  │  • Telegram Plugin: 内置支持                     │                │
│  └─────────────────────────────────────────────────┘                │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
          │                                  │
          │                                  │
          ▼                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                          外部服务层                                   │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐             │
│  │  Anthropic   │  │    Google    │  │    GitHub    │             │
│  │    Claude    │  │    Gemini    │  │      API     │             │
│  │   (AI 引擎)  │  │  (AI 引擎)   │  │  (Skills)    │             │
│  └──────────────┘  └──────────────┘  └──────────────┘             │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

---

## 网络拓扑

### Docker 网络配置

#### 自定义桥接网络

```yaml
networks:
  openclaw_net:
    driver: bridge
```

**特性：**
- **容器间通信**：所有容器通过服务名相互访问
- **DNS 解析**：自动解析容器名为 IP 地址
- **网络隔离**：与主机其他网络隔离
- **安全性**：容器仅暴露必要端口

#### 容器网络拓扑

```
openclaw_net (172.18.0.0/16)
├── traefik          (172.18.0.2)  → 0.0.0.0:80,443,8080
├── openclaw_gateway (172.18.0.3)  → 内部端口 18789,18790
└── cf_tunnel        (172.18.0.4)  → 无外部端口
```

### 端口映射策略

| 服务 | 内部端口 | 主机端口 | 用途 | 访问方式 |
|------|---------|---------|------|---------|
| Traefik | 80 | 0.0.0.0:80 | HTTP 入口 | 公开 |
| Traefik | 443 | 0.0.0.0:443 | HTTPS 入口 | 公开 |
| Traefik | 8080 | 0.0.0.0:8080 | Dashboard | 仅本地 |
| OpenClaw | 18789 | - | Gateway Core | 内部 |
| OpenClaw | 18790 | - | Socat Proxy | 内部 |
| Cloudflare Tunnel | - | - | 隧道连接 | 内部 |

**设计原则：**
- ✅ 仅 Traefik 暴露外部端口
- ✅ OpenClaw 完全隔离在内网
- ✅ Cloudflare Tunnel 通过 Traefik 访问服务

---

## 核心组件

### 1. Cloudflare Tunnel (cf_tunnel)

**角色：** 安全的公网访问入口

**技术规格：**
- **镜像**：`cloudflare/cloudflared:latest`
- **协议**：QUIC over UDP
- **数据中心**：khh01, tpe01 (台湾)
- **连接数**：4 个并发隧道连接

**配置：**
```yaml
cloudflared:
  image: cloudflare/cloudflared:latest
  command: tunnel run --token ${CF_TUNNEL_TOKEN}
  environment:
    - TUNNEL_TOKEN=${CF_TUNNEL_TOKEN}
  networks:
    - openclaw_net
  depends_on:
    - openclaw
```

**路由规则：**
```
公网域名 → http://traefik:80 → OpenClaw Service
```

**优势：**
- ✅ 零配置防火墙穿透
- ✅ 自动 HTTPS/SSL 加密
- ✅ DDoS 防护
- ✅ 全球 CDN 加速

---

### 2. Traefik (反向代理)

**角色：** HTTP/HTTPS 路由与负载均衡

**技术规格：**
- **版本**：v3.0
- **模式**：Docker Provider (自动服务发现)
- **TLS**：支持本地证书和 Let's Encrypt

**核心配置：**
```yaml
traefik:
  image: traefik:v3.0
  command:
    - "--api.insecure=true"
    - "--providers.docker=true"
    - "--providers.docker.exposedbydefault=false"
    - "--entrypoints.web.address=:80"
    - "--entrypoints.websecure.address=:443"
    - "--providers.file.filename=/etc/traefik/traefik_dynamic.yml"
  ports:
    - "80:80"
    - "443:443"
    - "8080:8080"
  volumes:
    - "/var/run/docker.sock:/var/run/docker.sock:ro"
    - "./certs:/etc/traefik/certs:ro"
    - "./traefik_dynamic.yml:/etc/traefik/traefik_dynamic.yml:ro"
```

**路由规则（动态配置）：**
```yaml
# OpenClaw 服务路由
labels:
  - "traefik.enable=true"
  - "traefik.http.routers.openclaw.rule=Host(`openclaw.local`) || Host(`localhost`) || Host(`openclaw.robby570.tw`)"
  - "traefik.http.routers.openclaw.entrypoints=web,websecure"
  - "traefik.http.services.openclaw.loadbalancer.server.port=18790"
```

**TLS 配置（traefik_dynamic.yml）：**
```yaml
tls:
  certificates:
    - certFile: /etc/traefik/certs/local-cert.pem
      keyFile: /etc/traefik/certs/local-key.pem
```

**Dashboard：**
- 访问地址：`http://localhost:8080`
- 功能：实时监控、路由检查、服务健康状态

---

### 3. OpenClaw Gateway (应用核心)

**角色：** AI Agent 核心引擎

**技术规格：**
- **运行环境**：Node.js 22 (Debian Bookworm)
- **架构**：WebSocket 服务器 + HTTP REST API
- **AI 引擎**：Multi-provider (Claude, Gemini, GPT)
- **通信渠道**：Telegram, WhatsApp, Slack, Email

**端口架构：**
```
┌─────────────────────────────────────┐
│     OpenClaw Gateway Container      │
├─────────────────────────────────────┤
│                                     │
│  ┌───────────────────────────────┐ │
│  │   OpenClaw Core Process       │ │
│  │   Port: 127.0.0.1:18789       │ │
│  │   Protocol: WebSocket + HTTP  │ │
│  └──────────┬────────────────────┘ │
│             │                       │
│             ▼                       │
│  ┌───────────────────────────────┐ │
│  │   Socat Proxy (暴露层)         │ │
│  │   Port: 0.0.0.0:18790         │ │
│  │   Mapping: 18790 → 18789      │ │
│  └───────────────────────────────┘ │
│                                     │
└─────────────────────────────────────┘
```

**为什么需要 Socat？**
1. **网络绑定**：OpenClaw 默认绑定 127.0.0.1（仅本地）
2. **Docker 访问**：其他容器需要通过 0.0.0.0 访问
3. **安全隔离**：核心进程不直接暴露

**启动脚本（start-openclaw.sh）：**
```bash
# 后台启动 OpenClaw Gateway
openclaw gateway --verbose &
OPENCLAW_PID=$!

# 启动 Socat 代理
socat TCP-LISTEN:18790,fork,reuseaddr,bind=0.0.0.0 TCP:127.0.0.1:18789 &
SOCAT_PID=$!

# 保持容器运行
wait
```

**数据持久化：**
```yaml
volumes:
  - ./data:/root/.openclaw          # 主配置目录
  - ./gog-config:/root/.config/gogcli  # Google Workspace 配置
```

**目录结构：**
```
/root/.openclaw/
├── agents/
│   └── main/
│       ├── agent/
│       │   ├── config.json
│       │   └── auth-profiles.json    # AI 认证配置
│       └── sessions/                  # 对话历史
├── workspace/
│   └── skills/
│       ├── bundled/                   # 内置 Skills
│       ├── managed/                   # 已安装 Skills
│       └── custom/                    # 自定义 Skills
├── canvas/                            # Web UI 资源
└── openclaw.json                      # 全局配置
```

---

### 4. Telegram 集成

**集成方式：** OpenClaw 内置 Telegram Plugin

**配置：**
```yaml
environment:
  - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
```

**工作原理：**
```
Telegram User
    │
    ▼
Telegram API Servers
    │ Long Polling
    ▼
OpenClaw Gateway (Telegram Plugin)
    │
    ▼
OpenClaw Agent (处理消息)
    │
    ▼
AI Providers (Claude/Gemini)
    │
    ▼
Telegram API (发送回复)
```

**功能特性：**
- ✅ 实时消息接收（Long Polling）
- ✅ 独立会话管理（每用户独立上下文）
- ✅ 命令系统（/start, /help, /status）
- ✅ Skills 调用支持

---

## 通信流程

### Web 访问流程（公网）

```
1. 用户浏览器
   │ HTTPS Request
   │ URL: https://your-domain.com/
   ▼
2. Cloudflare Edge (全球 CDN)
   │ SSL 终止
   │ DDoS 防护
   ▼
3. Cloudflare Tunnel (QUIC 加密)
   │ Token 认证
   │ 建立安全隧道
   ▼
4. cf_tunnel 容器
   │ HTTP 转发
   │ Destination: http://traefik:80
   ▼
5. Traefik 容器
   │ Host Header: your-domain.com
   │ 路由匹配规则
   ▼
6. openclaw_gateway:18790
   │ Socat 代理
   │ Forward to 127.0.0.1:18789
   ▼
7. OpenClaw Core (18789)
   │ WebSocket 握手
   │ 设备配对验证
   ▼
8. Web UI 渲染
   │ React 应用加载
   │ WebSocket 长连接建立
   ▼
9. 用户与 AI 交互
```

**性能指标：**
- 延迟：~100-200ms（包含全球 CDN）
- 带宽：Cloudflare 无限
- 并发：取决于容器资源

---

### 本地访问流程

```
1. 用户浏览器 (https://openclaw.local/)
   │ HTTPS (自签名证书)
   ▼
2. Traefik (443端口)
   │ TLS 终止
   │ Host: openclaw.local
   ▼
3. OpenClaw Gateway (18790)
   │ HTTP 处理
   ▼
4. OpenClaw Core (18789)
   │ WebSocket 连接
   ▼
5. AI 交互
```

---

### Telegram Bot 通信流程

```
1. 用户发送 Telegram 消息
   │
   ▼
2. Telegram API Servers
   │ Webhook 或 Long Polling
   ▼
3. OpenClaw Gateway (Telegram Plugin)
   │ 解析消息
   │ 提取用户 ID 和会话
   ▼
4. OpenClaw Agent
   │ 加载用户会话历史
   │ 调用 AI 引擎
   ▼
5. AI Provider (Claude/Gemini)
   │ 生成回复
   ▼
6. OpenClaw Gateway
   │ 格式化消息
   │ 调用 Telegram API
   ▼
7. Telegram API
   │ 发送消息给用户
   ▼
8. 用户收到回复
```

---

## 数据流分析

### HTTP/WebSocket 数据流

```
┌─────────────┐
│   Browser   │
└──────┬──────┘
       │ 1. HTTPS Request (TLS 1.3)
       │    Host: your-domain.com
       │    Path: /__openclaw__/canvas/
       ▼
┌─────────────┐
│  Cloudflare │
│    Edge     │
└──────┬──────┘
       │ 2. QUIC Tunnel (加密)
       │    Protocol: HTTP/2
       ▼
┌─────────────┐
│  cf_tunnel  │
└──────┬──────┘
       │ 3. HTTP (内部网络)
       │    Destination: http://traefik:80
       ▼
┌─────────────┐
│   Traefik   │
└──────┬──────┘
       │ 4. HTTP Proxy
       │    Backend: openclaw:18790
       ▼
┌─────────────┐
│   Socat     │
└──────┬──────┘
       │ 5. TCP Forward
       │    127.0.0.1:18789
       ▼
┌─────────────┐
│  OpenClaw   │
│   Gateway   │
└─────────────┘
       │ 6. WebSocket Upgrade
       │    Protocol: ws://
       ▼
   [持久连接建立]
       │
       │ 7. 双向消息流
       │    ┌─────────────┐
       │    │  AI Request │
       │    └─────────────┘
       │    ┌─────────────┐
       │    │ Tool Calls  │
       │    └─────────────┘
       │    ┌─────────────┐
       │    │  AI Reply   │
       │    └─────────────┘
       ▼
```

### AI 引擎交互流程

```
OpenClaw Gateway
    │
    ├─→ 1. 加载 auth-profiles.json
    │       ├─ anthropic:default
    │       ├─ google:default
    │       └─ openai:default
    │
    ├─→ 2. 根据模型配置选择引擎
    │       MODEL=anthropic/claude-3-haiku-20240307
    │
    ├─→ 3. 构建 AI 请求
    │       ├─ System Prompt
    │       ├─ User Message
    │       ├─ Conversation History
    │       └─ Available Skills/Tools
    │
    ├─→ 4. 调用 AI Provider API
    │       │ HTTPS Request
    │       ▼
    │   ┌─────────────────────────┐
    │   │  Anthropic Claude API   │
    │   │  api.anthropic.com      │
    │   └─────────────────────────┘
    │       │ JSON Response
    │       ▼
    │
    ├─→ 5. 解析 AI 响应
    │       ├─ Text Content
    │       ├─ Tool Calls (Skills)
    │       └─ Metadata
    │
    └─→ 6. 执行 Skills (如需要)
            │
            ├─→ GitHub Skill
            ├─→ Weather Skill
            ├─→ Calendar Skill
            └─→ Custom Skills
```

---

## 安全机制

### 1. 网络层安全

**Docker 网络隔离：**
```yaml
networks:
  openclaw_net:
    driver: bridge
    # 默认隔离：容器间通信需显式配置
```

**端口策略：**
- ✅ 最小暴露原则：仅 Traefik 暴露端口
- ✅ 内部通信：服务间通过 Docker DNS 访问
- ✅ 无直接访问：OpenClaw 不暴露任何外部端口

---

### 2. 传输层安全

**Cloudflare Tunnel：**
- ✅ QUIC 协议加密（TLS 1.3）
- ✅ 零信任架构（无需公网 IP）
- ✅ 自动 DDoS 防护

**Traefik TLS：**
```yaml
tls:
  certificates:
    - certFile: /etc/traefik/certs/local-cert.pem
      keyFile: /etc/traefik/certs/local-key.pem
```

**生产环境建议：**
```bash
# 使用 Let's Encrypt 自动证书
--certificatesresolvers.letsencrypt.acme.email=your@email.com
--certificatesresolvers.letsencrypt.acme.storage=/letsencrypt/acme.json
--certificatesresolvers.letsencrypt.acme.httpchallenge.entrypoint=web
```

---

### 3. 应用层安全

**设备配对机制：**
```
1. 浏览器发起连接
   │
   ▼
2. OpenClaw 生成配对请求
   │ Request ID: UUID v4
   │ Device Fingerprint: SHA-256
   │ Client IP: 记录
   │
   ▼
3. 等待管理员批准
   │ Command: openclaw devices approve <REQUEST_ID>
   │
   ▼
4. 颁发设备 Token
   │ Role: operator
   │ Scopes: operator.admin, operator.approvals
   │
   ▼
5. 浏览器保存 Token (LocalStorage)
   │
   ▼
6. 后续请求自动认证
```

**Token 管理：**
```bash
# 查看已配对设备
openclaw devices list

# 撤销设备权限
openclaw devices revoke <DEVICE_ID>

# 轮换 Token
openclaw devices rotate <DEVICE_ID>
```

---

### 4. API 密钥管理

**环境变量隔离：**
```yaml
environment:
  - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
  - GEMINI_API_KEY=${GEMINI_API_KEY}
  - GITHUB_TOKEN=${GITHUB_TOKEN}
  - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
```

**密钥存储：**
```
.env 文件 (主机)
    │ Docker 读取
    ▼
容器环境变量
    │ OpenClaw 启动脚本
    ▼
auth-profiles.json (容器内)
    │ OpenClaw Gateway 加载
    ▼
内存中使用 (运行时)
```

**最佳实践：**
- ✅ `.env` 文件加入 `.gitignore`
- ✅ 使用 `.env.example` 作为模板
- ✅ 定期轮换 API 密钥
- ✅ 最小权限原则（仅授予必需的 API 权限）

---

## 部署环境

### 系统要求

**操作系统：**
- ✅ Linux (Ubuntu 20.04+, Debian 11+)
- ✅ macOS (12.0+)
- ✅ Windows (WSL2)

**Docker 版本：**
- Docker Engine: 24.0+
- Docker Compose: 2.20+

**硬件要求：**
| 组件 | 最小配置 | 推荐配置 |
|------|---------|---------|
| CPU | 2 核心 | 4 核心 |
| 内存 | 2GB | 4GB |
| 磁盘 | 10GB | 20GB+ |
| 网络 | 1Mbps | 10Mbps+ |

---

### 环境变量配置

**必需变量：**
```bash
# Gateway 认证
OPENCLAW_GATEWAY_TOKEN=<强密码>

# 时区
TZ=Asia/Taipei
```

**AI 引擎（至少配置一个）：**
```bash
# Anthropic Claude
ANTHROPIC_API_KEY=sk-ant-api03-xxxxx

# Google Gemini (推荐，免费)
GEMINI_API_KEY=AIzaxxxxx

# OpenAI GPT
OPENAI_API_KEY=sk-proj-xxxxx
```

**通信渠道（可选）：**
```bash
# Telegram Bot
TELEGRAM_BOT_TOKEN=123456789:ABCxxxxx

# Slack
SLACK_BOT_TOKEN=xoxb-xxxxx
```

**Skills 配置（可选）：**
```bash
# GitHub
GITHUB_TOKEN=github_pat_xxxxx

# Notion
NOTION_API_KEY=secret_xxxxx
```

**网络配置（可选）：**
```bash
# Cloudflare Tunnel (公网访问)
CF_TUNNEL_TOKEN=eyJhxxxxx

# Google Workspace
GOG_KEYRING_PASSWORD=your_password
GOG_ACCOUNT=your@gmail.com
```

---

### 数据持久化

**Volume 映射：**
```yaml
volumes:
  - ./data:/root/.openclaw              # OpenClaw 数据
  - ./gog-config:/root/.config/gogcli   # Google CLI 配置
  - ./certs:/etc/traefik/certs          # TLS 证书
  - ./traefik_dynamic.yml:/etc/traefik/traefik_dynamic.yml  # Traefik 配置
```

**备份策略：**
```bash
# 备份配置和会话
tar -czf openclaw-backup-$(date +%Y%m%d).tar.gz \
  ./data \
  ./gog-config \
  .env

# 恢复
tar -xzf openclaw-backup-20260206.tar.gz
docker compose restart
```

---

## 监控与日志

### 日志查看

```bash
# 查看所有服务日志
docker compose logs -f

# 查看特定服务
docker compose logs -f openclaw
docker compose logs -f traefik
docker compose logs -f cloudflared

# 查看最近 100 行
docker compose logs --tail 100 openclaw
```

### Traefik Dashboard

访问：`http://localhost:8080`

**功能：**
- ✅ 实时路由状态
- ✅ 服务健康检查
- ✅ 请求统计
- ✅ 错误日志

---

## 性能优化

### 资源限制（可选）

```yaml
services:
  openclaw:
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
        reservations:
          cpus: '1'
          memory: 1G
```

### 缓存优化

```yaml
# Traefik 响应缓存
labels:
  - "traefik.http.middlewares.cache.plugin.httpcache.maxage=3600"
```

---

## 故障排除

### 常见问题诊断流程

```
问题发生
    │
    ▼
1. 检查容器状态
   docker compose ps
    │
    ├─ 容器未运行 → docker compose up -d
    │
    ▼
2. 查看日志
   docker compose logs --tail 50
    │
    ├─ 端口冲突 → 修改端口映射
    ├─ 权限错误 → chmod 600 certs/local-key.pem
    ├─ 网络错误 → docker network inspect openclaw_net
    │
    ▼
3. 测试网络连通性
   docker exec traefik wget -qO- http://openclaw:18790/
    │
    ├─ 连接失败 → 检查服务是否启动
    │
    ▼
4. 验证配置
   docker exec openclaw_gateway openclaw gateway status
    │
    ▼
5. 重启服务
   docker compose restart
```

---

## 升级指南

```bash
# 1. 备份数据
./scripts/backup.sh

# 2. 拉取最新镜像
docker compose pull

# 3. 重新创建容器
docker compose up -d --force-recreate

# 4. 验证服务
docker compose ps
npm run health
```

---

## 相关文档

- **[设备配对指南](./DEVICE_PAIRING.md)** - 详细的认证流程
- **[Cloudflare Tunnel 配置](./CLOUDFLARE_TUNNEL_SETUP.md)** - 公网访问设置
- **[Skills 管理](./SKILLS_MANAGEMENT.md)** - 扩展功能
- **[Telegram Bot 设置](./TELEGRAM_SETUP.md)** - 通信渠道配置

---

**文档维护：** 请在架构变更时同步更新本文档  
**反馈渠道：** GitHub Issues
